{
  "name": "asbuh-portal",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm run dev -w apps/web",
    "dev:api": "npm run dev -w apps/api",
    "build": "npm run build -w apps/web && npm run build -w apps/api",
    "lint": "npm run lint -w apps/web && npm run lint -w apps/api",
    "lint:fix": "npm run lint:fix -w apps/web && npm run lint:fix -w apps/api",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "npm run test -w apps/web && npm run test -w apps/api",
    "test:ci": "npm run test:ci -w apps/web && npm run test:ci -w apps/api",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.5.1",
    "prettier": "^3.5.3",
    "eslint": "^9.27.0",
    "@eslint/js": "^9.27.0",
    "globals": "^16.2.0",
    "eslint-plugin-react": "^7.37.5",
    "eslint-config-prettier": "^10.1.5"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,md,css,html,yml,yaml}": [
      "prettier --write"
    ]
  },
  "engines": {
    "node": ">=20"
  }
}
