# API Server
PORT=3001
NODE_ENV=development

# Database (PostgreSQL)
DATABASE_URL=postgresql://asbuh:asbuh_dev@localhost:5432/asbuh_portal

# JWT / Sessions
JWT_SECRET=change-me-in-production
JWT_ACCESS_EXPIRES=15m
JWT_REFRESH_EXPIRES=7d

# Admin seed (dev-only defaults, override in production)
ADMIN_EMAIL=admin@asbuh.local
ADMIN_PASSWORD=Admin123!
